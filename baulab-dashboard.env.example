# Copy to ~/.config/baulab-dashboard/env and edit.
# Source: EnvironmentFile in baulab-dashboard.service

# --- Cloudflare named tunnel (recommended: no rate limits, stable URL) ---
CLOUDFLARE_TUNNEL_TOKEN=your-token-from-cloudflare-dashboard
CLOUDFLARE_TUNNEL_URL=https://your-tunnel-hostname.example.com

# --- SSH key passphrase for git push (when using quick tunnels) ---
# Option 1: Path to file containing passphrase (chmod 600)
BAULAB_SSH_PASSPHRASE_FILE=/path/to/.config/baulab-dashboard/ssh-passphrase

# Option 2: Passphrase directly (less secure)
# BAULAB_SSH_PASSPHRASE=your-passphrase
